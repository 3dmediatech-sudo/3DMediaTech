const canvas=document.getElementById('networkCanvas');const ctx=canvas.getContext('2d');let particles=[];function resizeCanvas(){canvas.width=window.innerWidth;canvas.height=window.innerHeight;createParticles()}function createParticles(){const count=Math.min(95,Math.floor(window.innerWidth/16));particles=[];for(let i=0;i<count;i++){particles.push({x:Math.random()*canvas.width,y:Math.random()*canvas.height,vx:(Math.random()-.5)*.45,vy:(Math.random()-.5)*.45,size:Math.random()*2+1})}}function animate(){ctx.clearRect(0,0,canvas.width,canvas.height);particles.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>canvas.width)p.vx*=-1;if(p.y<0||p.y>canvas.height)p.vy*=-1;ctx.beginPath();ctx.arc(p.x,p.y,p.size,0,Math.PI*2);ctx.fillStyle='rgba(0, 204, 255, 0.75)';ctx.fill()});for(let i=0;i<particles.length;i++){for(let j=i+1;j<particles.length;j++){const dx=particles[i].x-particles[j].x;const dy=particles[i].y-particles[j].y;const distance=Math.sqrt(dx*dx+dy*dy);if(distance<135){ctx.beginPath();ctx.moveTo(particles[i].x,particles[i].y);ctx.lineTo(particles[j].x,particles[j].y);ctx.strokeStyle=`rgba(0, 204, 255, ${1-distance/135})`;ctx.lineWidth=.8;ctx.stroke()}}}requestAnimationFrame(animate)}window.addEventListener('resize',resizeCanvas);resizeCanvas();animate();const loadButton=document.getElementById('loadVideo');if(loadButton){loadButton.addEventListener('click',function(){let link=document.getElementById('videoLink').value.trim();let videoId='';if(link.includes('youtube.com/watch?v=')){videoId=link.split('v=')[1].split('&')[0]}else if(link.includes('youtu.be/')){videoId=link.split('youtu.be/')[1].split('?')[0]}else{alert('Only approved media links are supported in this demo.');return}document.getElementById('videoBox').innerHTML=`<iframe width="700" height="394" src="https://www.youtube.com/embed/${videoId}" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>`})}
