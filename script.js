const canvas=document.getElementById('networkCanvas');if(canvas){const ctx=canvas.getContext('2d');let pts=[];function resize(){canvas.width=innerWidth;canvas.height=innerHeight;pts=[];for(let i=0;i<Math.min(150,Math.floor(innerWidth/10));i++)pts.push({x:Math.random()*canvas.width,y:Math.random()*canvas.height,vx:(Math.random()-.5)*.35,vy:(Math.random()-.5)*.35,r:Math.random()*1.7+.6})}function draw(){ctx.clearRect(0,0,canvas.width,canvas.height);pts.forEach(p=>{p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>canvas.width)p.vx*=-1;if(p.y<0||p.y>canvas.height)p.vy*=-1;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle='rgba(0,150,255,.75)';ctx.fill()});for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){let dx=pts[i].x-pts[j].x,dy=pts[i].y-pts[j].y,d=Math.hypot(dx,dy);if(d<130){ctx.beginPath();ctx.moveTo(pts[i].x,pts[i].y);ctx.lineTo(pts[j].x,pts[j].y);ctx.strokeStyle=`rgba(0,150,255,${(1-d/130)*.55})`;ctx.stroke()}}requestAnimationFrame(draw)}addEventListener('resize',resize);resize();draw()}
const nav=document.querySelector('.navbar');document.querySelector('.menu-toggle')?.addEventListener('click',()=>nav.classList.toggle('open'));
const glow=document.querySelector('.cursor-glow');document.addEventListener('mousemove',e=>{if(glow){glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'}});
document.querySelectorAll('.card,.stat-card,.roadmap-panel,.demo-panel,.contact-box,.timeline-card').forEach(el=>el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect();el.style.setProperty('--mx',((e.clientX-r.left)/r.width*100)+'%');el.style.setProperty('--my',((e.clientY-r.top)/r.height*100)+'%')}));
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.14});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const loadButton=document.getElementById('loadVideo');if(loadButton){loadButton.addEventListener('click',()=>{const input=document.getElementById('videoLink'),box=document.getElementById('videoBox'),link=input.value.trim();let id='';if(link.includes('youtube.com/watch?v='))id=link.split('v=')[1].split('&')[0];else if(link.includes('youtu.be/'))id=link.split('youtu.be/')[1].split('?')[0];else{box.innerHTML='<div class="empty-player"><div class="play-circle">!</div><p>Only approved YouTube links are supported in this demo.</p></div>';return}box.innerHTML=`<iframe src="https://www.youtube.com/embed/${id}" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>`})}
