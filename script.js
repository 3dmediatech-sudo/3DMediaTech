const nav=document.querySelector('.navbar'),menu=document.querySelector('.menu-toggle');if(menu)menu.onclick=()=>nav.classList.toggle('open');
const reveals=document.querySelectorAll('.reveal');const io=new IntersectionObserver(e=>e.forEach(x=>{if(x.isIntersecting)x.target.classList.add('show')}),{threshold:.12});reveals.forEach(r=>io.observe(r));
const c=document.getElementById('networkCanvas'),ctx=c.getContext('2d');let w,h,pts=[];function size(){w=c.width=innerWidth;h=c.height=innerHeight;pts=Array.from({length:70},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.35,vy:(Math.random()-.5)*.35}))}size();addEventListener('resize',size);function draw(){ctx.clearRect(0,0,w,h);for(const p of pts){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;ctx.fillStyle='rgba(0,198,255,.75)';ctx.beginPath();ctx.arc(p.x,p.y,1.4,0,7);ctx.fill()}for(let i=0;i<pts.length;i++)for(let j=i+1;j<pts.length;j++){let a=pts[i],b=pts[j],d=Math.hypot(a.x-b.x,a.y-b.y);if(d<130){ctx.strokeStyle=`rgba(0,145,255,${(1-d/130)*.23})`;ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}requestAnimationFrame(draw)}draw();
const load=document.getElementById('loadVideo'),input=document.getElementById('videoLink'),box=document.getElementById('videoBox');if(load&&input&&box){load.onclick=()=>{let v=input.value.trim(),id='';try{let u=new URL(v);if(u.hostname.includes('youtu.be'))id=u.pathname.slice(1);else id=u.searchParams.get('v')||''}catch(e){}box.innerHTML=id?`<iframe src="https://www.youtube.com/embed/${id}" allowfullscreen></iframe>`:'<p>Please paste a valid YouTube link.</p>'}}
