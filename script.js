const canvas=document.getElementById('networkCanvas');
const ctx=canvas.getContext('2d');let w,h,points=[];function resize(){w=canvas.width=innerWidth;h=canvas.height=innerHeight;points=Array.from({length:Math.min(95,Math.floor(w*h/14000))},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.45,vy:(Math.random()-.5)*.45,r:Math.random()*1.8+1}))}resize();addEventListener('resize',resize);
function draw(){ctx.clearRect(0,0,w,h);for(const p of points){p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>w)p.vx*=-1;if(p.y<0||p.y>h)p.vy*=-1;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle='rgba(0,170,255,.85)';ctx.shadowColor='#009bff';ctx.shadowBlur=10;ctx.fill()}ctx.shadowBlur=0;for(let i=0;i<points.length;i++){for(let j=i+1;j<points.length;j++){const a=points[i],b=points[j],dx=a.x-b.x,dy=a.y-b.y,d=Math.hypot(dx,dy);if(d<150){ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.strokeStyle=`rgba(0,150,255,${(1-d/150)*.38})`;ctx.lineWidth=1;ctx.stroke()}}}requestAnimationFrame(draw)}draw();
const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
const load=document.getElementById('loadVideo');if(load){load.addEventListener('click',()=>{const input=document.getElementById('videoLink');const box=document.getElementById('videoBox');let url=input.value.trim();let id='';if(url.includes('youtube.com/watch'))id=new URL(url).searchParams.get('v');if(url.includes('youtu.be/'))id=url.split('youtu.be/')[1].split('?')[0];if(id){box.innerHTML=`<iframe width="760" height="430" src="https://www.youtube.com/embed/${id}" title="3D Media demo" allowfullscreen></iframe>`}else{box.innerHTML='<p style="margin-top:22px;color:#7ecbff;font-weight:800">Please paste an approved YouTube media link.</p>'}})}
